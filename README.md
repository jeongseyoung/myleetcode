# myleetcode   
  
Arrays.asList(arr).stream().distinct().toList();   리스트 중복제거


result.put(s, result.getOrDefault(s, 0) + 1); map에 값넣기, 키값에 s가 없으면 value에 0(디폴트값), 있으면 +1 

“중복 제거 = Set”

“빈도 계산 = Map”

“문자 뒤집기 = StringBuilder + reverse or loop”

“검색 빨리 = Hash 구조”

“정렬 = 그때그때 Comparator”

“contains는 왜 느린가?”

“왜 getOrDefault가 편한가?”


map을 set형태로     
  
   
     
       
   
